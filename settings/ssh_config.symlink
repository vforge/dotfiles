Host *
  # Use a portable terminal for SSH clients that bypass the interactive zsh
  # wrapper (scripts, rsync, scp, and similar tools). OpenSSH 8.7+ supports
  # SetEnv; the remote zshrc also guards against a forwarded xterm-ghostty.
  SetEnv TERM=xterm-256color
